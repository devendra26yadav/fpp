package Lession3.day2.StaticBlock;

// static block will be executed only one time but in order they appear when class loads
// inside static block only static methods and static instance will be called
public class StaticBaby {
}
